package nn.dgord.patterns.strategy.utils;

import java.util.UUID;

public class Constants {
    public static final UUID USER_ID_FIRST = UUID.randomUUID();
    public static final UUID USER_ID_SECOND = UUID.randomUUID();
}
